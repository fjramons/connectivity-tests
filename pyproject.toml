[project]
name = "connectivity-tests"
version = "0.1.0"
description = "Generation of specifications and manifests to validate connectivity between Remote cloud domain and Local cloud domain"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
