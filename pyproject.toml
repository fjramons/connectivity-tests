[project]
name = "connectivity-tests"
version = "0.1.0"
description = "Generacion de especificaciones y manifiestos para validar conectividad Domain 2 <-> Domain 3 (EC.3)"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
